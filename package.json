{
  "name": "ses-lambda",
  "version": "1.0.0",
  "description": "This service sends out emails using SES and with API Gateway. And can be used to send out notification/mail for some transactional activity.",
  "main": "handler.js",
  "dependencies": {
    "aws-sdk": "^2.1153.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Paras Jain",
  "license": "ISC"
}
